- Fixed terminal Copy replacing the selection with surrounding TUI content (#639). The smart-copy panel detection matched any multi-row selection inside a full-screen TUI like zellij and copied whole rows between the pane borders instead of the highlighted text. Border stripping now applies only when the selection crosses a border column. Also fixed Copy writing an empty clip when the selection had scrolled out of the snapshot.